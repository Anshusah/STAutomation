@using Cicero.Service.Models.SimpleTransfer.User
@using Cicero.Service.Models.Core;
@model SenderIdentityViewModel
@{
    ViewData["Title"] = "Sender Identity";
    ViewData["body"] = "inner-pg";
    Theme theme = ViewData["theme"] as Theme;
    Layout = "~/Themes/"+theme.GetName(false) + "/Shared/_Layout.cshtml";
}

@await Html.PartialAsync("../Shared/_Header.cshtml")
<div class="account-access signup--mod">
    <div class="container">
        <div class="account-access__wrapper">
            <h3 class="text-center mb-5">Verify your Identity</h3>
            <h5 class="text-center mb-5">Select the type of document you would like to upload</h5>
            <form method="post">
                @Html.AntiForgeryToken()
                <div class="status"></div>
                <div class="clearfix"></div>
                @*<div asp-validation-summary="All" class="text-danger mt-2"></div>*@
                <button type="button" class="btn btn-primary btn-block">Step1. Verify Identity</button>

                <button type="button" class="btn btn-primary btn-block">Step1. Verify Identity</button>

                <button type="button" class="btn btn-primary btn-block">Step1. Verify Identity</button>

                <small class="form-text text-muted mb-4">After  complettion all third party steps user will redirect to register steps screens</small>

                <button type="submit" class="btn btn-primary btn-block">Save</button>
            </form>
        </div>
    </div>
</div>
@await Html.PartialAsync("../Shared/_Footer.cshtml")
@section Scripts{}